// !Lesson  2
// In this lesson we will learn Build-in-module (node.js provides us many in-build modules)
// (node.js own module)--> http, url, path, fs (no need to install)
// fs means file system module (this function is async function)
//fs module--> writeFile(), appendFile(), readFile(), rename(), unlink(), Exist()